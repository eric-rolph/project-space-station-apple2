{
  "name": "project-space-station",
  "version": "1.0.0",
  "description": "Project: Space Station (1985) – Apple IIe game running on Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler deploy --config wrangler.toml",
    "dev:worker": "npx wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "typescript": "^5.8.0",
    "vite": "^6.3.0",
    "wrangler": "^4.10.0"
  }
}
